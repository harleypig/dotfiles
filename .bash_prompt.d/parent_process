
function __parent_process() {

  local parents=$(pstree -sA $PPID)
  parents=${parents/%-+*/}
  parents=${parents//bash---/}
  parents=${parents/systemd---slim---openbox---}
  parents=${parents/%---pstree/}
  parents=${parents//---/ : }

  echo "${text_red}${text_blink}(${parents})${text_reset} "

}
