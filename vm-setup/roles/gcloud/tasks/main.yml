---
# This playbook uses the add_repo role to install the Google Cloud repository
- name: Include add_repo to install Google Cloud repository
  include_role:
    name: add_repo
  vars:
    repo_list:
      - google-cloud

- name: Install Google Cloud SDK packages
  include_role:
    name: install_pkgs
  vars:
    package_groups: "gcloud"
