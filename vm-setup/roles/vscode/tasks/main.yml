---
# This playbook uses the add_repo role to install the Microsoft repository
- name: Include add_repo to install Microsoft repository
  include_role:
    name: add_repo
  vars:
    add_repo_repo_list:
      - microsoft

# Install Visual Studio Code package
-
  include_role:
    name: install_pkgs
  vars:
    install_pkgs_package_groups: vscode
