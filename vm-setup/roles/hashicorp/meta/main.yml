---
dependencies:
  - role: add_repo
    repo_list:
      - hashicorp

dependencies:
  - role: add_repo
    vars:
      repo_list:
        - hashicorp
  - role: install_pkgs
    vars:
      packages:
        - terraform
        - packer
        - terraform-ls
