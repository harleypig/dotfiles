argument_specs:
  version_tracking_action:
    type: str
    choices:
      - load
      - update
      - save
    required: True
    description:
      - "Specify the action for version tracking: 'load', 'update', or 'save'."
