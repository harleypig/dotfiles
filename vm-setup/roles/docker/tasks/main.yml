---
- name: Remove older Docker packages
  become: true
  apt:
    name:
      - docker
      - docker-engine
      - docker.io
      - containerd
      - runc
    state: absent
    purge: true

- name: Install Docker packages
  package:
    name: "{{ docker_packages }}"
    state: present
