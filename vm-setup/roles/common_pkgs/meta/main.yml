---
dependencies:
  - role: add_repo
    vars:
      filename: 'glow'
      key_url: 'https://repo.charm.sh/apt/gpg.key'
      repo_name: 'Glow'
      repo_urls:
        - 'deb https://repo.charm.sh/apt/ * *'
