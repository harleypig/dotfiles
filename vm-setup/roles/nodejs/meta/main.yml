---
dependencies:
  - role: add_repo
    vars:
      repo_list:
        - nodesource

  - { role: ansible.builtin.package, vars: { name: "{{ nodejs_packages }}", state: "present" } }
    vars:
      packages:
        - nodejs
