- name: Common Packages
  hosts: localhost
  become: yes

  #debugger: on_failed

  tasks:
    - name: Install Common Packages
      apt:
        state: latest
        update_cache: yes
        name:
          - ansible
          - ansible-lint
          - aptitude
          - ca-certificates
          - curl
          - dconf-editor
          - dos2unix
          - elinks
          - expect
          - git
          - jq
          - markdown
          - mlocate
          - pandoc
          - parallel
          - tmux
