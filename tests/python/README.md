# Python tests

pytest tests for the repo's Python scripts (e.g. `bin/poetry2setup`,
`config/claude/hooks/rule-coverage.py`).

- Files: `test_*.py`
- Run: `pytest tests/python`

See `.claude/TESTS.md` for this repo's test layout and
`config/claude/rules/testing.md` for the multi-language test conventions.
