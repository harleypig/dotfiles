#!/bin/bash

[[ -f "$HOME/.nochecklinks" ]] && exit 0
[[ -z $DOTFILES ]] && exit 0

links_file="$DOTFILES/default-dotlinks"
[[ -r "$HOME/.dotlinks" ]] && links_file="$HOME/.dotlinks"
[[ -r $links_file ]] || exit 0

cd || exit 0

readarray -t link_files < <(grep -v '^[[:space:]]*#' "$links_file" | envsubst)

for link_file in "${link_files[@]}"; do
  [[ -z $link_file ]] && continue

  if [[ ! -e $link_file ]]; then
    ln -s "$link_file" .

  else
    linked_file="$(readlink -nf "$link_file")"
    [[ $linked_file != "$link_file" ]] && echo "$link_file is not linked to $DOTFILES"
  fi
done
