#!/bin/bash

# Compare with https://github.com/koalaman/shellcheck/wiki/Contrib#docker-wrapper-script

exec docker run \
  --rm \
  --user "$(id -u):$(id -g)" \
  --volume "$PWD:/mnt" \
  --workdir "/mnt" \
  koalaman/shellcheck:stable "$@"
