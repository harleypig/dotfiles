alias mkvenv="type -t deactivate &> /dev/null; \
  rm -fr venv; \
  python3 -m venv --upgrade-deps venv; \
  [[ -f requirements.txt ]] && pip install -r requirements.txt \
  "

alias wondertwins="source venv/bin/activate"
