# Taskwarrior program configuration file
# Use the command 'task show' to see all defaults and overrides

# This is updated automatically by Taskwarrior
news.version=3.4.2

data.location=$PROJECTS_DIR/private_dotfiles/.task

# Single include for all configuration
#include $PROJECTS_DIR/private_dotfiles/.task/include/main.config
