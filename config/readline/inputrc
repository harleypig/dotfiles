# http://www.reddit.com/r/commandline/comments/kbeoe/you_can_make_readline_and_bash_much_more_user/
# https://wiki.archlinux.org/index.php/Readline

$include /etc/inputrc

# Ctrl + L

# Space then Esc
"C-e": history-expand-line

# Mode Settings
# Settings related to the editing mode and visual indicators for the mode.
#
set editing-mode vi
set show-mode-in-prompt on
set vi-ins-mode-string "\001\033[32m\002|INS|\001\033[0m\002 "
set vi-cmd-mode-string "\001\033[41m\033[93m\002<CMD>\001\033[0m\002 "

# Completion Settings
# Settings that affect how command line completion is handled.

set colored-completion-prefix on
set colored-stats on
set completion-ignore-case on
set completion-map-case on
set completion-prefix-display-length 2
set mark-symlinked-directories on
set show-all-if-ambiguous on
set show-all-if-unmodified on
set skip-completed-text on
set visible-stats on

# Ctrl + I Show possible completions
"C-i": menu-complete

# History Settings
# Settings that affect the behavior of command history.

set history-preserve-point on

# Bind up and down arrow keys for history search
"\e[A":history-search-backward
"\e[B":history-search-forward

# Meta Settings
# Settings related to the handling of meta characters (characters with the eighth bit set).

set input-meta on
set output-meta on

# Tilde Expansion Settings
# Settings that control the expansion of the tilde character '~' in the command line.

set expand-tilde on

# Key Bindings
# Custom key bindings for various readline actions.
