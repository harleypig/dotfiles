# Deny everything; allow only tracked config. Each config/ subdir owns its
# own allowlist in this format (deny all, then re-include specifics).
# See config/.gitignore.

*
!.gitignore
!cargo
!docker
!gh
!git
!npm
!packwiz
!poetry
!proj
!README.md
!rustup
